extends layout

block content
  h1= title
  p Assignment 10 with the changes in index file.
  body 
  button(type="button",class="btn btn-outline-info" onclick="doBump()") Press Me
  div#bump Bump the count 
  a(href="umbrella") Umbrella 
  script.  
    var count = 0;
    var bump = 1;
    function  doBump(){
      count = count + bump;
      bump = bump + 1 ;
      document.getElementById("bump").innerHTML = "Count"+ count+"bump"+bump 
    } 